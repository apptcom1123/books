# 前端資料存取與使用者體驗工程綱領

## 一、文件目的

本綱領適用於使用 React、Next.js、Vue 等前端框架，並透過 Supabase 或其他 API 後端讀寫資料的專案。新進工程師在開發任何列表、文章、留言、搜尋、按讚、通知或即時協作功能時，都應遵循本文件。

我們的目標不是單純讓 API「可以取得資料」，而是讓使用者在不同裝置與網路條件下，都能清楚知道系統正在做什麼，並獲得快速、穩定且一致的操作體驗。所有設計應圍繞三個核心：減少實際等待時間、降低主觀等待感，以及確保前端畫面與伺服器資料最終一致。

## 二、核心原則

第一，畫面不可因資料載入而大幅跳動。首次載入時應使用符合實際版型的骨架屏，並預留圖片、卡片和文字區塊尺寸，以降低 CLS。已有快取資料時，不應重新清空畫面改放骨架屏；應保留舊資料，在背景重新驗證，必要時僅顯示低干擾的更新提示。

第二，使用者操作必須立即獲得回饋。按讚、收藏、切換狀態等低風險操作，可以採用樂觀更新：先更新畫面，再於背景送出請求。若請求失敗，必須回復正確狀態並提供可理解的提示。涉及金額、庫存、權限或不可逆行為時，不可只依賴前端推測結果，應等待伺服器確認。

第三，只傳輸與渲染真正需要的資料。禁止在沒有理由的情況下使用 `select(*)`、一次載入完整資料集，或在列表中的每個項目分別查詢關聯資料。資料欄位、筆數、圖片尺寸與訂閱範圍都應根據畫面需求縮減。

## 三、資料請求規範

彼此沒有依賴的請求應平行執行，避免形成瀑布式載入；具有關聯的資料則優先透過 relation select、View 或 RPC 整合，避免 N+1 Query。首頁及主要操作區只載入首屏必要內容，次要統計、推薦內容與非關鍵資訊可延後取得。

搜尋輸入應使用 debounce，捲動、拖曳或 resize 等高頻事件應使用 throttle。然而防抖本身不能解決請求競態：使用者快速變更關鍵字時，舊請求可能比新請求更晚完成。因此必須取消過時請求，或以 request ID 確保只有最後一次結果可以更新畫面。

所有大型列表都必須分頁。一般後台表格可以使用 offset 分頁；動態消息、留言、通知等持續新增的資料，應優先使用以 `created_at` 加 `id` 組成的游標分頁，以避免新資料插入後產生重複或遺漏。無限捲動應在接近頁尾時預取下一頁，同時保留「載入更多」或其他可操作的替代方式。

## 四、快取與資料生命週期

專案應統一使用 React Query、SWR 或同等機制管理伺服器狀態，不要在每個元件中自行拼湊 `useEffect` 和 loading 狀態。每一類資料都應定義穩定的 query key、合理的 stale time，以及重新抓取條件。

首次進入且沒有資料時顯示骨架屏；已有資料而正在背景更新時保留原畫面；更新失敗時也應盡可能保留舊資料。返回剛瀏覽過的頁面時應優先呈現快取，而非重新開始完整載入。對使用者下一步極可能開啟的內容，可以在滑鼠停留、連結進入可視區域或路由切換前進行預取，但需避免無限制預取造成頻寬浪費。

資料異動後，應精準更新或失效對應的 query，不可為了方便而清除整個應用程式快取。登出時則必須清除與帳號相關的敏感快取，避免不同帳號共享舊資料。

## 五、Realtime 與樂觀更新

Realtime 的作用是加速同步，不是取代資料庫查詢。訂閱事件必須依主鍵合併與去重，不可收到 INSERT 就無條件加入列表，也不可收到 UPDATE 就盲目對數字再加一次。尤其樂觀更新成功後，伺服器可能再次透過 Realtime 推送同一筆異動；此時應以伺服器回傳的完整資料覆蓋本地狀態，或使用 mutation ID 辨識事件。

元件卸載時必須取消訂閱。訂閱中斷並重新連線後，應重新驗證關鍵查詢，因為斷線期間的事件可能遺失。高頻事件應批次更新，避免每收到一筆推播就重新渲染整個列表。

快速連按或同時送出多次 mutation 時，應停用重複提交、合併操作或排隊處理。計數器、瀏覽數與按讚數等操作應由資料庫函式或 RPC 原子更新，避免前端「先讀取、加一、再寫回」造成資料競爭。

## 六、渲染與媒體效能

大量資料不等於大量 DOM。長列表應採用虛擬化，只渲染使用者目前看得到的項目。列表 key 必須使用穩定主鍵，不可使用陣列 index。全域狀態只保存跨頁面真正需要共享的資訊，避免一個小變更觸發整頁重繪。

Supabase Storage 中的圖片應提供適當尺寸的縮圖，預留寬高並使用懶載入；使用者要求查看時才載入原圖。私有檔案的 signed URL 應在有效期限內重用，不可每次 render 都重新產生。大型上傳應顯示進度，並提供取消、失敗重試或續傳能力。

使用 Next.js 時，首屏關鍵資料可由伺服器取得，以改善首次內容呈現及 SEO；將伺服器資料交給客戶端快取時，要正確 hydration，避免瀏覽器啟動後立刻重複請求。

## 七、錯誤、離線與權限狀態

畫面必須區分首次載入、背景更新、空資料、離線、逾時、未登入、沒有權限與伺服器錯誤。禁止用同一句「發生錯誤」處理全部情況。錯誤訊息應說明目前影響與下一步，例如重新嘗試、重新登入或聯絡管理員。

網路錯誤及部分 5xx 可使用有限次數的指數退避重試；400、401、403 等錯誤通常需要修正請求、更新登入狀態或處理權限，不應無限重試。驗證登入狀態期間不可先顯示未登入頁面，以免產生閃爍；但前端隱藏功能僅是介面控制，真正的資料權限必須由 Supabase RLS 保護。

## 八、資料庫與後端責任

常用於 `WHERE`、`ORDER BY`、JOIN 及 RLS Policy 的欄位應建立適當索引。複合索引需符合實際查詢順序，而非看到欄位就任意建立。開發者應檢查慢查詢、查詢計畫、回傳筆數及 payload 大小。複雜統計或重複計算可移至 View、Materialized View 或 RPC，但仍需評估更新成本與權限邊界。

任何 service role key 都不得出現在瀏覽器端。前端可見的 anon key 必須與正確的 RLS Policy 配合；所有資料表上線前都要測試一般使用者、未登入使用者及不同帳號之間的讀寫隔離。

## 九、可觀測性與驗收標準

效能優化不能只憑開發者主觀感覺。團隊應持續觀察 API 延遲、錯誤率、慢查詢、payload 大小、快取命中率、Realtime 重連次數，以及 LCP、CLS、INP 等前端指標。重要 mutation 應記錄成功、失敗與回滾比例，以找出真正影響使用者的流程。

每一個資料功能提交 PR 前，工程師至少應確認：首次載入有穩定佔位；已有資料時背景更新不會清空畫面；請求欄位和筆數經過限制；搜尋不存在競態；列表具備分頁；錯誤可以重試且不會丟失既有內容；樂觀更新可以正確回滾；Realtime 不會重複資料；訂閱會被清理；RLS 與索引經過驗證；慢速網路、離線和不同登入狀態皆已測試。

完成上述條件，才代表功能不只是「能運作」，而是達到可交付的工程品質。新人若不確定該採用哪一種策略，應先確定資料規模、更新頻率、一致性要求與失敗成本，再與負責工程師確認；不要以增加 loading spinner 作為所有效能問題的預設答案。
